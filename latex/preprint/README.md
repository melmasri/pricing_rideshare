# Preprint

Place preprint LaTeX sources here (e.g. arXiv submission, conference version).

The canonical manuscript lives in [`../paper/`](../paper/). Copy or symlink `main.tex`, `references.bib`, and `img/` as needed, then adjust formatting for the target venue.
